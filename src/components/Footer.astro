---
import { tabElements } from '../utils/data/data';
import { IconOnlypacks } from './icons/react/outline';

const getCurrentYear = (): number => {
    const currentDate = new Date();
    return currentDate.getFullYear();
};
---

<div class="max-w-screen-lg mx-auto px-4 py-12 text-center">
    <h1 class="text-sm text-neutral-400 font-semibold">El mayor sitio de packs porno XXX del mundo</h1>
    <p class="mt-3 text-xs text-neutral-400">OnlyPacks te ofrece packs porno gratuitos ilimitados sin acortadores. Disfruta de la comunidad de porno amateur más grande de la red, así como de escenas completas de los mejores estudios XXX. Actualizamos nuestros packs porno diariamente para asegurarnos de que siempre obtengas packs de la mejor calidad.</p>
</div>

<footer class="bottom-0 border-t border-neutral-800 rounded-lg shadow bg-neutral-900">
    <div class="max-w-screen-lg mx-auto p-4 xs:py-8">
        <div class="xs:flex xs:items-center xs:justify-between">
            <a href="/" class="flex mb-4 xs:mb-0" aria-label="App Logo"> 
                <IconOnlypacks className="h-7 xs:h-9"/>
            </a>
            <ul class="flex flex-wrap items-center gap-4 xs:gap-6 mb-6 text-sm font-medium text-neutral-400 xs:mb-0">
                {tabElements.map((tab) => (
                    <li>
                        <a href={tab.href} class="hover:underline">{tab.name}</a>
                    </li>
                ))}
            </ul>
        </div>
        <hr class="my-6 border-neutral-800 xs:mx-auto xs:my-8" />
        <span class="block text-sm text-neutral-400 xs:text-center">© {getCurrentYear()} <a href="/" class="hover:underline">Onlypacks™</a>. All Rights Reserved.</span>
    </div>
</footer>
